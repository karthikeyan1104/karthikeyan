#include<stdio.h>
#include<conio.h>
void main()
{
int main()
{
int arr[Min_SIZE];
int ,h;
printf("\n enter size of array:");
scanf("%d" ,&N);
printf("enter %d elements in the array :", N);
for(I=0; i<N; i++)
{
scanf("%d" ,&arr[i]);
}
printf("enter %d elements in the array : ", N);
for(I=0; i<N; i++)
{
scanf("%d", &arr[i]);
}
printf("\n Elemts in array are: ");
for(i=0; i<N; i++)
{
printf("%d, ", arr[i]);
getch();
}
