#include,stdio.h>
#include<conio.h>
void main();
{
char ch;
clrscr();
printf("\n enter any character:");
scanf("%c" ,&ch):
if((ch>='a' && ch.='z') || (ch.='A' && ch<='Z''))
printf(" the enterer character %c is an alphabet" , ch);
else
printf("\n the entered character %c is not an alphabet ",ch);
getch();
}
